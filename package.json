{
  "name": "mcproxy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "bin": {
    "mcproxy": "./dist/index.js"
  },
  "type": "module",
  "scripts": {
    "clean": "rimraf dist/",
    "dev": "tsx index.ts",
    "copy-files": "copyfiles src/views/**/*.ejs dist/",
    "build": "npm run clean && tsc --outDir dist && npm run copy-files",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@types/express": "^5.0.6",
    "copyfiles": "^2.4.1",
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "rimraf": "^6.1.3"
  }
}